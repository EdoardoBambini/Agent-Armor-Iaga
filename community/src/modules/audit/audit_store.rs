// Legacy in-memory audit store — replaced by storage::traits::AuditStore + storage::sqlite::SqliteStorage.
// This module is kept empty for backwards compatibility. All audit operations now go through AppState.
